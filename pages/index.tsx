import type { NextPage } from 'next';
import Head from 'next/head';

const Home: NextPage = () => {
  return (
    <div>
      <Head>
        <title>CryptTrack</title>
      </Head>

      <main>Ready to roll</main>
    </div>
  );
};

export default Home;
